language: go

env:
  - GO111MODULE=on

go:
  - "1.11.x"
  - "1.12.x"
  - "tip"

before_install:
  - go get github.com/mattn/goveralls

install:
  - go mod download
  
script:
  - $HOME/gopath/bin/goveralls -service=travis-ci
